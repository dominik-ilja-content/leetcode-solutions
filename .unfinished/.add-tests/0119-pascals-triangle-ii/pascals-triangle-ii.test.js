const getRow = require("./pascals-triangle-ii");

describe("tests for leecode 119 - pascals triangle ii", () => {
  it("", () => {
    let setup = null;
    let expected = null;
    let result = null;
    expect(result).toBe(expected);
  });
});
