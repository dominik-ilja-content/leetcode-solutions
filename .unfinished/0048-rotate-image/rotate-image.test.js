const rotate = require("./rotate-image");

describe("tests for leecode 48 - rotate iamge", () => {
  describe("should return correct result for different inputs", () => {
    it("", () => {
      let setup = null;
      let expected = null;
      let result = null;
      expect(result).toBe(expected);
    });
  });

  it("", () => {
    let setup = null;
    let expected = null;
    let result = null;
    expect(result).toBe(expected);
  });
});
