const isStrictlyPalindromic = require("./strictly-palindromic-number");

describe("tests for leecode 2396 - strictly palindromic number", () => {
  it("", () => {
    let setup = null;
    let expected = null;
    let result = null;
    expect(result).toBe(expected);
  });
});
